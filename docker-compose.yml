version: "3.8"
services:
  cache:
    image: "redis:latest"
    expose:
      - "6379"
  api:
    build: .
    volumes:
      - type: bind
        source: .
        target: /app
    ports:
      - "3000:3000"
    command:
      - ./run.sh
    depends_on:
      - "cache"
